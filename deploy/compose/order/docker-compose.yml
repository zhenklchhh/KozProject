version: '3.9'
services:
  postgres:
    image: postgres:17.0-alpine3.20
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - core-network

volumes:
  pgdata:

networks:
  core-network:
    name: core-network
    external: true